# Canonical language entry point (no trailing slash — source of truth)
/ /en 308

# Unsupported languages are permanently redirected to en (see sudobility/docs/SEO.md).
/ar /en 308
/ar/ /en 308
/ar/* /en/:splat 308

# SPA fallback for all other routes (history, credits, login, settings, …).
# Document routes with a committed snapshot in /html are handled earlier by
# functions/_middleware.js (it injects the snapshot into the live index.html);
# everything else falls through to the per-route static file or the SPA here.
/* /index.html 200
